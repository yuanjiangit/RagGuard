package com.aizerohub.ragguard.junit5;

/**
 * Marker for the JUnit 5 module. The {@code @RagTest} annotation,
 * {@code RagAssertions} and the extension implementation land in M1.
 */
final class ModuleMarker {

    private ModuleMarker() {
    }
}
